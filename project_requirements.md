I need a system that can do following;
1. Allow users to upload a requirement document (PDF, DOCX, TXT)
2. Parse the document and extract requirements and categorize them as title,requirement and detect item numbers using AI
3. Save this document as original and structured format in database under a project name. (project should be created via dashboard)
4. should allow user to create derivative document from that parsed document like system requirement specification like described in mil-std-498
5. all derived document should be linked in original document in requirement level
6. user should change derived document and save it as new version of document (this should be tracked in database.). versioning should work in requirement level and document level.
7. requirement editing should be done in structured format. Allow inline editing and AI support. (open chat  window to edit requirement with AI support. for instance there will be button on requirement on hover and when user clicks it it will appear as reference in chat window and user can edit it with AI support. ) 
8. Editing should allow adding new requirement with also AI support. All requirements should be linked to derived document in requirement level. 
9.Documents should be in draft,review or publish state. Published documents are freezed version of documents with major version numbers.
10. System should allow pdf export with custom templates. 
create a implementation plan